{
  "name": "figma-ai-design-generator-plugin",
  "version": "1.0.0",
  "description": "Figma plugin that generates designs from natural language prompts",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2015",
    "watch": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2015 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.106.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.7.3"
  }
}
